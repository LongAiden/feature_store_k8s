# Add the necessary repositories
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo add debezium https://debezium.io/helm-charts/ # Note: Debezium not in provided Chart.yaml dependencies, but might be used otherwise.
helm repo add jenkins https://charts.jenkins.io
helm repo add influxdata https://helm.influxdata.com/
helm repo add grafana https://grafana.github.io/helm-charts

# Refresh your local chart index
helm repo update

# Then, install your chart
helm install feature-store-k8s ./my-project-chart --namespace fs-k8s-project